version: '3.8'
services:
  web:
    image: warnex/storeapi:latest
    ports:
      - "5000:80"
  redis:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf
      - redis-data:/data

volumes:
  redis-data:
